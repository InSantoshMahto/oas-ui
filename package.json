{
  "name": "@insantoshmahto/oas-ui",
  "version": "0.1.0",
  "description": "OpenAPI UI runner and adapters (experimental). Provides a pluggable system for serving OpenAPI docs (Swagger UI, ReDoc, Scalar) for Nest and Express apps.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "echo \"No tests configured\""
  },
  "keywords": [
    "nestjs",
    "openapi",
    "oas",
    "swagger",
    "redoc",
    "documentation"
  ],
  "author": "InSantoshMahto",
  "license": "MIT",
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "typescript": "latest"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
